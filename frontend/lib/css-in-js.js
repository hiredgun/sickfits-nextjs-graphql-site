import styled, { css, ThemeProvider } from 'styled-components';
import { prop, ifProp, switchProp } from 'styled-tools';

export default styled;
export { css, prop, ifProp, switchProp, ThemeProvider };
